#include <gtk/gtk.h>

static void
print_hello (GtkWidget *widget,
             gpointer   data)
{
  printf("Hello World\n");
}

static void
activate (GtkApplication *app, gpointer user_data){
  GtkWidget *window;
  GtkWidget *buttonStart;
  GtkWidget *buttonStart_box;
  GtkWidget *titre;

  window = gtk_application_window_new (app);
  gtk_window_set_title (GTK_WINDOW (window), "Window");
  gtk_window_set_default_size (GTK_WINDOW (window), 800, 500);

  //BOXE
  GtkWidget* gtk_hbox_new(fals
e, 1);

  //TITRE
  titre=gtk_label_new("¿2vine ki C?");
  gtk_container_add(GTK_CONTAINER(window), titre);

  //BOUTON COMMENCER
  buttonStart_box = gtk_button_box_new (GTK_ORIENTATION_HORIZONTAL);
  gtk_container_add (GTK_CONTAINER (window), buttonStart_box);

  buttonStart = gtk_button_new_with_label ("JOUER");
  g_signal_connect (buttonStart, "clicked", G_CALLBACK (print_hello), NULL);
  gtk_container_add (GTK_CONTAINER (buttonStart_box), buttonStart);

  //AJOUT DES ELEMENTS A LA BOXE
   gtk_box_pack_start(GTK_BOX(), pHBox, TRUE, TRUE, 0);

  gtk_widget_show_all (window);
}

int
main (int    argc,
      char **argv)
{
  GtkApplication *app;
  int status;

  app = gtk_application_new ("org.gtk.example", G_APPLICATION_FLAGS_NONE);
  g_signal_connect (app, "activate", G_CALLBACK (activate), NULL);
  status = g_application_run (G_APPLICATION (app), argc, argv);
  g_object_unref (app);

  return status;
}
